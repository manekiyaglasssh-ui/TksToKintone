# 未確認事項

対象アプリ：**TKS OLAP to kintone**（v1.6.2 / バージョンコード46）

> ソースコードからは判断できない、または業務上の確認が必要な事項をまとめる。各項目に根拠ファイルを付す。

---

## 1. 業務上の意味が不明な項目

| # | 事項 | 根拠ファイル | 補足 |
| --- | --- | --- | --- |
| 1-1 | 加工種類コード（1=四方, 2=長2, …, 11=3方）の業務上の正確な定義 | `app/preview_state.py`（`KAKOU_TYPE_NAMES`） | コードと表示名の対応は分かるが業務的意味は要確認 |
| 1-2 | OP区分（00/01/02）の業務上の意味と分岐の妥当性 | `app/voucher_data_mapper.py`（`resolve_unit_and_amount_values`） | 面積/単価/金額の算出分岐の根拠は業務確認が必要 |
| 1-3 | 「移動伝票（取引区分=8）」の判定・表示制御の業務要件 | `app/voucher_service.py`（`is_move_slip_transaction_type`） | コード上の閾値・表示差分の意図 |
| 1-4 | 得意先ヘッダーの判定文字列（既定「エレベータ」）の運用方針 | `app/customer_header.py` | 得意先2〜4が空既定の運用理由 |
| 1-5 | 加工名マスタの各列（メーカー識別掛率集計コード等）の入力ルール | `app/kakou_master.py` | `DEFAULT_MAKER_CODE="MK"` の付与規則 |

---

## 2. テスト・コメントから推測した挙動

| # | 事項 | 根拠 | 補足 |
| --- | --- | --- | --- |
| 2-1 | 既存kintoneデータの反映ルール（受注No単位/行単位、㎡非反映）は「要件4〜7」コメント由来 | `app/kintone_existing.py` | 要件番号の元資料は未確認 |
| 2-2 | 二重起動制御（`activate_existing_instance`）の具体的な仕組み（QLocalServer等） | `app/gui.py`（`run_gui`） | `run_gui` 全体は未精読。単一インスタンス検出方法は要確認 |
| 2-3 | PDFプレビューでPyMuPDF/QtPdf両方が無い場合の挙動 | `app/voucher_preview_window.py` | フォールバック先が無い場合の表示は未確認 |
| 2-4 | 詳細設定（R2条件オーバーライド）の具体的な業務影響 | `app/gui.py`（`AdvancedSettingsDialog`/`_load_r2_conditions`） | 設定変更がOLAP取得結果に与える影響範囲 |
| 2-5 | kintone登録先（test/production）の切替UIの所在 | `app/gui.py`（`_kintone_target`） | 設定画面での切替方法は精読範囲外 |

---

## 3. 呼び出し元・利用箇所が確認できていないもの

| # | 事項 | 根拠 | 補足 |
| --- | --- | --- | --- |
| 3-1 | `app/update_helper.py`（with-helperビルド）の実運用での起動経路 | `app/update_helper.py` | 通常ビルドでは未使用と推測。実際の配布形態は要確認 |
| 3-2 | `tks_to_kintone/cli.py`（CLIエントリ）の運用有無 | `tks_to_kintone/cli.py` | GUIとは別系統。利用実態は未確認 |
| 3-3 | `templates/voucher_olap_request.json` と `docs/olap/*.json` の役割分担 | `app/config.py`, `app/voucher_data_mapper.py` | 前者はマッピング元、後者は実リクエストと推測 |

---

## 4. 設定値の変更可否・反映範囲が未確認のもの

| # | 事項 | 根拠 | 補足 |
| --- | --- | --- | --- |
| 4-1 | コードに同梱されたWebhook URL・APIトークンの管理方針 | `app/teams_notifier.py`, `app/update_client.py`, `templates/config.env.sample` | 機密値がリポジトリに含まれる。ローテーション/秘匿の運用要確認 |
| 4-2 | `PREVIEW_COLOR_THEME=auto` の安定性 | `templates/config.env.sample` | コメントに「環境によって不安定」とあり要目視確認 |
| 4-3 | `VOUCHER_RECORD_RETENTION_DAYS`（既定1095日）など保存期間の運用妥当性 | `app/voucher_settings.py` | ディスク使用量・運用要件と整合するか |
| 4-4 | `TKS_VOUCHER_OLAP_DISABLE_OP_FIELDS` と `_ENABLED_OP_FIELDS` の優先関係の実運用 | `app/config.py`, `templates/config.env.sample` | 後者優先だが現場の設定状況は未確認 |

---

## 5. PDF・帳票レイアウトの目視確認が必要なもの

| # | 事項 | 根拠 | 補足 |
| --- | --- | --- | --- |
| 5-1 | サンプル紙伝票（`img/sample_denpyou_*.pdf`）との完全一致 | `app/voucher_service.py`, `app/voucher_templates.py` | 座標定数は多数。実出力との突合は目視 |
| 5-2 | CIDフォント（HeiseiKakuGo-W5）での記号（✔等）のベクター描画の見え方 | `app/voucher_service.py`（`draw_symbol_text`） | 記号字形を持たないため線描画。再現性は目視 |
| 5-3 | 加工指図書印刷（A4横モノクロ）のレイアウト・QRコード | `app/print_service.py`, `app/print_layout.py` | 実機印刷での見切れ・濃度 |
| 5-4 | 指図書編集オブジェクトのscene→reportlab座標変換の精度 | `app/voucher_edit_objects.py`, `app/voucher_service.py` | 編集位置とPDF位置の一致は目視 |

---

## 6. 環境・配布に関する未確認事項

| # | 事項 | 根拠 | 補足 |
| --- | --- | --- | --- |
| 6-2 | keyring未導入環境でのパスワード保存（QSettings平文フォールバック）の許容範囲 | `app/credential_store.py` | セキュリティ運用方針 |
| 6-3 | 非Windows環境での動作（パス・印刷・タイトルバーテーマ） | `app/path_utils.py`, `app/theme_utils.py` | 主にWindows前提と推測 |
| 6-4 | Inno Setup インストーラ（`installer/tks-to-kintone.iss`）の署名・配布フロー | `installer/tks-to-kintone.iss`, `app/update_client.py` | 署名済みインストーラ要件の運用 |

---

## 7. 解析範囲の限界

- `app/gui.py`（3119行）・`app/voucher_edit_window.py`（5662行）・`app/voucher_service.py`（2667行）は主要メソッドのシグネチャと代表的なフローを確認したが、全行は精読していない。細部の分岐・例外処理に本書未記載の挙動がある可能性がある。
- `tests/`（多数）には仕様固定のテストが存在する。本書作成時は個別テスト本文を精読していないため、テストで固定された詳細挙動は別途 `tests/` を参照のこと（例：`test_voucher_data_mapper.py`, `test_kintone_existing.py`, `test_area_calculator.py`, `test_voucher_edit_objects.py` ほか）。
- `docs/` 配下に既存の説明書（`操作説明書_v1.4.10.md`, `画面項目説明書_v1.4.10.md`, `SPEC.md` 等）があり、業務的背景はそちらも参照すると補完できる。
