# 変更履歴

## 1.6.0 (44)

- バージョンを 1.6.0（44）へ更新。

## 1.5.13 (43)

- デバッグ表示時に、更新確認用KintoneのアプリIDとAPIトークンを設定できるようにした。設定は更新一覧とインストーラ取得の両方へ反映し、デバッグ表示OFF時は本番設定を使用する。
- 更新確認用のデバッグAPIトークンを資格情報ストアへ保存。片方だけ・不正値を設定エラーとし、デバッグ接続の通信失敗時に本番へ自動切替しないようにした。
- 指図書編集の未保存プレビューを伝票一覧と同じ対象・順序・PDF生成経路へ統一し、選択済みの複数伝票を共通編集・伝票No別編集込みで表示するよう修正。
- 伝票中央右側の受注No・伝票Noで、ASCII大文字I／全角Ｉの直後に4ptの字間を入れ、補正幅を右寄せ位置と枠内縮小へ反映。
- SumatraPDFの導入方式を、アプリ配下のポータブル版同梱から独立したWindowsアプリとしてのインストール方式へ変更。
- 固定版の公式SumatraPDF installerをTksToKintoneセットアップへ収録し、新規インストール時と更新時に未導入の場合だけ自動導入するよう変更。
- 更新後にSumatraPDFが見つからず印刷できない問題を修正。
- 明示設定、HKCU、HKLM、LOCALAPPDATA、Program Filesの順で、実在するSumatraPDF.exeを印刷時に自動検出するよう変更。
- SumatraPDFが後から削除された場合、印刷ジョブを失敗として確定し、セットアップ再実行を案内するよう改善。
- バージョンを 1.5.13（43）へ更新。

## 1.5.12 (42)

- 伝票へ表示する加工名を1～12行目ごとに変更可能にした。内部加工キー・判定・Kintone登録値は従来どおり維持する。
- 指図書編集で、ドラッグ矩形へ収まる文字サイズの自動計算と、テキストのリサイズハンドルによる文字サイズ変更に対応。
- テキストの右クリック「編集」と、小さい文字・細い線・小さいオブジェクトの拡張ヒット領域を追加。
- お気に入りオブジェクトへscene座標と基準ページ寸法を保存し、登録位置へ復元するよう変更。
- 指図書編集画面から未保存の編集内容を直接プレビューできる機能を追加。
- 反映先とお気に入りをセクション内でドラッグ＆ドロップ並び替えできるようにし、stable IDの順序をQSettingsへ保存。
- 指図書編集のプレビュー、短文テキスト編集、リサイズに関する不具合を修正。
- バージョンを 1.5.12（42）へ更新。

## 1.5.11 (41)

- 伝票へ表示する加工名を設定画面から変更可能にした。内部加工キー・判定・Kintone登録値は従来どおり維持する。
- 指図書編集で、ドラッグ矩形へ収まる文字サイズの自動計算、右クリック「編集」、小さい文字の拡張ヒット領域を追加。
- お気に入りオブジェクトへscene座標と基準ページ寸法を保存し、登録位置へ復元するよう変更。
- 指図書編集画面から未保存の編集内容をプレビューできる機能を追加。
- バージョンを 1.5.11（41）へ更新。

## 1.5.10 (40)

- 日本語フォント、TTC内のスタイル別フェイス選択、文字ごとのglyph fallbackを見直し、指図書のPDF・プレビュー・印刷でフォントと文字装飾をより正確に反映するよう改善。
- 指図書編集画面の背景生成とフォント一覧取得を遅延・キャッシュし、画面を先に操作できるよう起動を高速化。
- 伝票一覧は最初の10件を表示した時点で操作可能にし、残りを10件単位で読み込むよう変更。
- Kintone登録前確認画面を先に表示し、OLAP・Kintone確認と後続行準備をバックグラウンド化。全件確認完了まで登録を無効にする安全性を維持。
- PDF生成・プレビュー・印刷時に保存済みの最新編集内容を再読込し、古い編集内容やプレビューキャッシュが使われる問題を修正。
- バージョンを 1.5.10（40）へ更新。

## 1.5.9 (39)

- OP区分が `00`、`01`、`02` の明細だけ、単価列・金額列の上段に㎡を表示するようにした。
- 指図書編集で、テキストのフォント、文字サイズ、太字を選択できるようにした。
- 選択したテキスト書式を保存・再読込、Undo／Redo、伝票間コピー、PDF出力へ反映するようにした。
- 指図書編集のフォントへお気に入り登録・解除を追加。お気に入りと全フォントを1つの見出し付き一覧へ統合し、登録内容はアプリ設定へ保存する。存在しないフォントは安全に整理する。
- 指図書編集の太字・斜体・下線・取り消し線を1つの文字装飾メニューへ集約し、ショートカットと右クリックからも操作可能にした。装飾は保存・コピー・Undo／Redo・PDFへ反映する。
- フォントお気に入りの☆／★をフォント欄の左へ移動し、透明背景・枠線なしのコンパクト表示にした。
- バージョンを 1.5.9（39）へ更新。

## 1.5.8 (38)

- 受注Noの範囲指定で、`C405113` のような英字プレフィックス付き受注Noに対応。英字部分を保持し、末尾の数字を連番として一括取得できるようにした。
- `AB001` のような先頭ゼロを含む英字付き受注Noの範囲指定に対応。
- バージョンを 1.5.8（38）へ更新。

## 1.5.7 (37)

- 伝票の単価・明細金額・金額列合計表示を「従来条件／常に表示／常に非表示」から選べる設定を追加。
- 数字のみ／英字プレフィックス付き受注Noの範囲指定取得（最大500件）、非同期進捗、成功・失敗結果コピーを追加。
- アプリ内のQt標準右クリック編集メニューを日本語化。
- バージョンを 1.5.7（37）へ更新。

## 1.5.6 (36)

- バージョンを 1.5.6（36）へ更新。

## 1.5.5 (35)

- 登録前確認画面にCSV列設定を追加し、出力列の順序と出力対象を保存できるようにした。
- バージョンを 1.5.5（35）へ更新。

## 1.5.4 (34)

- 伝票PDFヘッダーのコードNo/伝票No/受注No の値が「I」で始まる場合、値の描画位置を少し右へずらして左枠線や隣接項目との重なりを解消（枠線・ラベル・他項目位置は不変）。全角「Ｉ」もNFKC正規化で対象化。売上伝票・工場控・指図書(1)(2)・梱包明細書・配送指示書・納品書・受領書（01〜08）に適用。
- バージョンを 1.5.4（34）へ更新。

## 1.5.2 (32)

- 伝票作成・印刷画面ヘッダーのボタンラベルを「表示設定」から「設定」に変更（動作は統合設定ダイアログのまま）。
- 表示設定タブの列表示/非表示で、固定列を「□」「受注No」「OLAP」の3つに限定。それ以外（右端の操作ボタン列を含む）を表示/非表示で選択可能にした。
- 統合設定ダイアログのOK押下後に出ていた「現在の一覧にもこの設定を反映しますか？」確認ダイアログを削除。
- 伝票設定タブ下部に「現在の一覧に反映する」チェックボックスを追加（既定OFF・都度リセット）。ON時のみ現在の一覧へ伝票設定を反映する。
- 機能選択画面の「TKS受注No取込」ボタンをデバッグ表示OFFでも常に表示するよう変更。
- 指図書編集画面の上部メニューがダークテーマで読めない問題を修正（ツールバー・コンテナ・図形メニューの配色をテーマ対応）。
- バージョンを 1.5.2（32）へ更新。
